const input = `2
I am happy today
We want to win the first prize`;

let split = input.split('\n');
const originalArr = [];
const imitation = [];
for (let i = 1; i <= split[0]; i++) {
    originalArr.push(split[i]);
}
for (let i = 0; i < originalArr.length; i++) {
    const test = originalArr[i];
}
